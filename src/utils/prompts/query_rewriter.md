Given the following conversation history and a follow-up question, rephrase the follow-up question to be a standalone question.
If the follow-up question is already standalone, return it as is.
Do NOT answer the question, just rewrite it.
Include necessary context from the history (e.g. "it", "that", "the function") replacing pronouns with specific names or concepts discussed.

Chat History:
{history}

Follow-up Question: {question}

Standalone Question: